import os
from dotenv import load_dotenv

load_dotenv()

# --- ВАЛИДАЦИЯ КОНФИГУРАЦИИ ---
TG_TOKEN = os.getenv("TG_TOKEN")
NEURO_KEY = os.getenv("NEURO_KEY")
WEBHOOK_URL = os.getenv("WEBHOOK_URL", "")  # Теперь берется из env, пустая строка по умолчанию

if not TG_TOKEN:
    raise ValueError("Критическая ошибка: Переменная окружения TG_TOKEN не найдена!")
if not NEURO_KEY:
    raise ValueError("Критическая ошибка: Переменная окружения NEURO_KEY не найдена!")

# Настройки бота
BOT_NAME = "Bible Bot v2.0"
ADMIN_IDS = [int(x) for x in os.getenv("ADMIN_IDS", "").split(",") if x.isdigit()]

# Системный промпт для AI
SYSTEM_PROMPT = """
Ты — христианский библейский бот-помощник. Твоя задача — помогать пользователям понимать Библию, 
находить утешение и мудрость в Писании.
Твои ответы должны быть:
1. Библейски точными.
2. Теплыми и ободряющими.
3. Структурированными (используй списки и жирный шрифт для удобства чтения).
4. Краткими, но содержательными (не более 300 слов за раз, если не просят подробнее).

Если пользователь спрашивает о грехе или сложных моральных вопросах, отвечай с любовью, 
но не отступай от библейских принципов. Не осуждай, но указывай на путь покаяния и надежды.
"""

# Ограничения
MAX_MESSAGE_LENGTH = 4096
COOLDOWN_SECONDS = 5
